<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Daily Watch News Digest — Archive</title>
{% include "_style.html.j2" %}
</head>
<body>
<div class="container">
  <header>
    <h1>Daily Watch News Digest</h1>
    <div class="meta">Archive — all daily digests, most recent first</div>
    <p class="description">Summary of daily news from major watch media outlets. Fully automated and AI-generated; errors may occur. The feed runs daily at 6:00PM CET. For feedback, please open a <a href="https://github.com/roman807/watch_news/issues/new" target="_blank" rel="noopener">GitHub issue</a>.</p>
  </header>

  {% if entries %}
    <ul class="archive-list">
      {% for entry in entries %}
        <li><a href="{{ entry.filename }}">{{ entry.display }}</a></li>
      {% endfor %}
    </ul>
  {% else %}
    <p class="empty">No digests published yet.</p>
  {% endif %}
</div>
</body>
</html>
